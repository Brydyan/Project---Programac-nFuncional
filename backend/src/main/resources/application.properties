# Firebase service account credentials path (absolute or relative to app working dir)
firebase.credentials.path=backend/secrets/archivospv-firebase-adminsdk-fbsvc-e0fd965468.json

# Firebase Storage bucket name (adjust if your bucket uses a different domain)
# Common values: archivospv.appspot.com or archivospv.firebasestorage.app
firebase.bucket=archivospv.firebasestorage.app

# Other app properties can go here
# ====== PUERTO BACKEND ======
server.port=${SERVER_PORT:8080}

# ====== MONGO ======
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://root:mi_clave_secreta@localhost:27017/YARGFlow?authSource=admin}

# ====== REDIS ======
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}

# ====== JWT ======
jwt.secret=${JWT_SECRET:CAMBIA_ESTE_VALOR_EN_DEV}
jwt.expiration-hours=${JWT_EXPIRATION_HOURS:24}

# Use forwarded headers from reverse proxy (X-Forwarded-For, etc.)
server.use-forward-headers=true

# Allow uploads up to 50MB (backend will still enforce image <=10MB and other files <=50MB)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
